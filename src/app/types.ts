export enum CellStatus {
  CORRECT = 'CORRECT',
  INCORRECT = 'INCORRECT',
  ABSENT = 'ABSENT',
  UNSUBMITTED = 'UNSUBMITTED',
}

export enum GridRowType {
  COMPLETED = 'COMPLETED',
  CURRENT = 'CURRENT',
  EMPTY = 'EMPTY',
}
